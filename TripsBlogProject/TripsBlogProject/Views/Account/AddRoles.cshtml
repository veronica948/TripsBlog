@model TripsBlogProject.Models.UserRolesViewModel
@{
    ViewBag.Title = "AddRoles";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>AddRoles</h2>
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.User.Id)
    @*@Html.CheckBox("group1", new Dictionary<string, object> { { "id", 1 } }) <span>fdjfkdfjk</span>*@

    <div class="form-group">
        @Html.LabelFor(model => model.Roles, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @foreach (var role in Model.Roles.Items)
            {
                @Html.CheckBox("SelectedRoles", role.Selected, new { value = role.Value });
                <span>@role.Text</span>
            
                
                @*@<string, object> { { "id", role.Id } }) <span>@role.Name</span>*@
                @*@Html.EditorFor(model => role, new { htmlAttributes = new { @class = "form-control" } })*@
            }
            @*@Html.ValidationMessageFor(model => model.Roles, "", new { @class = "text-danger" })*@
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
    <div>
        @Html.ActionLink("Back to List", "AllUsers")
    </div>
}

